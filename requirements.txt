streamlit
streamlit-elements==0.1.*
opencv-python-headless
mediapipe
numpy
gTTS
pygame
# SpeechRecognition  # Requires microphone access - only for local development
flask
flask-cors
# pyaudio  # Requires system dependencies - only for local development
scikit-learn

# Note: For local development with speech features, install separately:
# pip install pyaudio SpeechRecognition
